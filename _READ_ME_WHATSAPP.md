# مركز الواتساب — التعديلات (مشروع Admin)

استخرج هذا الزِب **داخل مجلد Admin** → كل ملف ينزل بمكانه.
كل شي مدفوع على GitHub ومتحقّق (`tsc` + `build` أخضر). لم يُلمس الدفع ولا FCM.

---

## الملفات المعدّلة (2)

### 1) `server.ts`

**أ) رابط التتبع كان يوجّه العميل لدومين الأدمن** ← السبب الجذري:
```
ALTURATH_TRACK_BASE_URL = env || ALTURATH_ADMIN_BASE_URL || ALTURATH_CUSTOMER_BASE_URL
                                 ^^^^^^^^^^^^^^^^^^^^^^ احتياط غلط
```
صار يرجّح موقع العميل → `https://alturathkw.shop/track`.
⚠️ لو متغيّر البيئة `ALTURATH_TRACK_BASE_URL` مضبوط على دومين الأدمن، فهو يتجاوز هذا الإصلاح — تأكد منه.

**ب) "ما شفت ولا قاعدة" — السبب: ما كان فيه أي قواعد افتراضية إطلاقاً.**
المجموعة في Firestore فاضية، ولا يوجد قواعد مدمجة بالكود. أضفت:
- **16 قاعدة جاهزة**: ترحيب، منيو، تتبع، توصيل، مناطق، أوقات، دفع، أسعار، عروض، موقع، حلال/مكونات، شكر + **تحويل لموظف** للشكاوى والولائم والإلغاء وطلب موظف.
- نقطة `POST /api/whatsapp/auto-replies/seed` تركّبها. **أي قاعدة عندك تُتخطّى ولا تُدهس** (إلا لو أرسلت `force=1`).
- القواعد تستخدم متغيّرات ديناميكية: `{menu_link}` `{track_link}` `{order_link}` `{customer_phone}`.

**ج) الرسائل كانت تُجلب "أقدم 200"** (`orderBy createdAt asc`) → في محادثة طويلة تشوف الأقدم **وتختفي الجديدة**. صارت **أحدث 200** ثم تُعكس زمنياً للعرض.

### 2) `src/components/WhatsAppSupportInbox.tsx`
- زر **"✨ القواعد الجاهزة"** (في عنوان القسم + في حالة الفراغ) يركّب الباقة بضغطة.
- حالة الفراغ كانت تقول *"سيستخدم البوت الردود الافتراضية الذكية"* — **معلومة مضلّلة** (ما فيه ردود افتراضية). صارت دعوة واضحة للتركيب.

---

## ⚠️ باقي مفتوح — يحتاج قرارك

1. **🔴 ثغرة أمنية حرجة (مؤكّدة حيّة):** نقاط `/api/whatsapp/*` **بدون أي حماية**. أي شخص يقرأ محادثات عملائك ويرسل واتساب باسم مطعمك. الحل: بوابة تتحقق من **Firebase ID token** (سيرفر + واجهة معاً).
2. **🔴 توقيع الويبهوك غير محقّق** (`x-hub-signature-256`) → يمكن حقن رسائل واردة وهمية.
3. **💸 الاستطلاع مكلف:** رسائل كل **1 ث** + محادثات كل **1.2 ث** → آلاف قراءات Firestore بالدقيقة لكل تبويب. الحل: `onSnapshot` (لحظي وأرخص بكثير).
4. **✨ مزايا مقترحة:** بحث داخل المحادثات، وسوم/تصنيف، إسناد لموظف، ربط المحادثة بالطلب، قوالب واتساب الرسمية.
