# تشغيل بوت واتساب التراث الآن

## 1) رفع تعديل السيرفر

ضع `server.ts` في جذر مشروع الأدمن بدل النسخة الحالية، ثم ارفع خدمة الأدمن بنفس طريقة النشر المعتادة عندكم. لا تغيّر خدمة الدفع ولا إعدادات الإشعارات.

الخدمة المشار إليها في إعداد المشروع هي:

- Cloud Run service: `service`
- Region: `europe-west3`
- Project: `gen-lang-client-0200723670`

## 2) إنشاء سر الربط

على الماك افتح Terminal داخل `whatsapp-web-bridge` وشغّل:

```bash
node generate-secret.mjs
```

انسخ الرقم الطويل الناتج واحفظه مؤقتاً.

## 3) تفعيل وضع Web Bridge في Cloud Run

من Google Cloud Console:

1. افتح Cloud Run ثم الخدمة `service`.
2. اختر **Edit and deploy new revision**.
3. من **Variables & Secrets** أضف أو حدّث:
   - `WHATSAPP_TRANSPORT` = `web_bridge`
   - `WHATSAPP_BRIDGE_SECRET` = السر الطويل الذي أنشأته
4. لا تحذف ولا تغيّر أي متغير موجود، خصوصاً متغيرات الدفع والإشعارات.
5. اضغط Deploy.

بعد النشر افتح:

```text
https://admin.alturathkw.shop/api/whatsapp/health
```

المفروض يظهر:

- `transport: web_bridge`
- `bridgeConfigured: true`
- `firebaseReady: true`

## 4) تجهيز الماك

1. افتح مجلد `whatsapp-web-bridge`.
2. شغّل `install-mac.command` مرة واحدة.
3. افتح ملف `.env` الذي ينشئه المثبت.
4. ضع نفس السر في `WHATSAPP_BRIDGE_SECRET` واحفظ الملف.
5. داخل Terminal في نفس المجلد شغّل:

```bash
npm run check
```

يجب أن تظهر رسالة أن الربط الآمن جاهز.

## 5) ربط واتساب وتشغيل البوت

1. افتح `start-mac.command`.
2. في أول تشغيل يظهر QR داخل النافذة.
3. من الهاتف: واتساب > الإعدادات > الأجهزة المرتبطة > ربط جهاز.
4. امسح QR.
5. انتظر ظهور: `بوت التراث جاهز`.

الجلسة تحفظ داخل `.session`، لذلك لا تحتاج QR في كل مرة. لا ترسل هذا المجلد لأي شخص.

## 6) تجربة صحيحة

أرسل من رقم آخر، وليس من نفس رقم الشركة:

1. `السلام عليكم`
2. `1`
3. اسم منتج موجود
4. `وين طلبي؟`
5. `موظف`

بعد كلمة `موظف` افتح **مركز واتساب الذكي** في برنامج التراث وجرب الرد اليدوي. الرد اليدوي يدخل الطابور ويرسله الماك.

## 7) التشغيل الاحترافي

بعد نجاح الاختبار شغّل:

`install-autostart-mac.command`

سيعمل الجسر تلقائياً عند دخولك للماك، ويعاد تشغيله عند الانقطاع. يجب أن يبقى الماك شغالاً ومتصلاً بالإنترنت وغير نائم. للإلغاء استخدم `uninstall-autostart-mac.command`.

## إيقاف التجربة والرجوع

1. أوقف نافذة الجسر بـ `Control + C`.
2. غيّر `WHATSAPP_TRANSPORT` في Cloud Run إلى `cloud`.

هذا يعيد مسار الواتساب الرسمي من دون لمس الدفع أو الإشعارات.
