# إصلاحات 2026-08-29 — server.ts فقط

الملف المعدّل الوحيد: **server.ts** (كلا الإصلاحين بداخله).
⚠️ يحتاج إعادة نشر/تشغيل السيرفر حتى يفعّل. لم يُرفع شيء.

---

## ١) مسج الدفع يوصل مرتين

**السبب الجذري:** كل عملية دفع كانت ترسل الإشعار من مسارين بنفس اللحظة بمعرّفَي pushEvents مختلفين، فطبقة منع التكرار ما تعتبرهم نفس الحدث:
- `announcePaidPaymentInstantly` → eventId فيه لاحقة زمنية: `safe-worker-invoice-paid-INV-5097@2026...`
- الـ webhook (`handlePaymentUpdate`) يرسل مرة ثانية inline بمعرّف **بدون** اللاحقة: `safe-worker-invoice-paid-INV-5097`

**التعديل:** حذف الإرسال الـ inline الزائد في `handlePaymentUpdate` (للفاتورة ~سطر 6726 وللطلب ~سطر 6768) مع إبقاء تحديث حالة الدفع في قاعدة البيانات. الإشعار الآن يُرسل مرة واحدة فقط عبر المرسِل الفوري.

## ٢) سجل الفواتير ما يعرض كل الفواتير (يقتصر على ~300)

**السبب الجذري:** `loadFullAppDataShard` ترمي استثناء عند أي عدم تطابق لحظي بين الـ manifest والأجزاء في الـ shard المجزّأ (يحدث بعد أي حفظ لأن الكتابة غير ذرّية). وبما أن `initBootCache` كانت تحمّل كل الـ shards في `Promise.all` واحد، فشل shard الفواتير كان يُفشل الكاش كله، فيرجع الـ endpoint `/api/appdata/full` النسخة الاحتياطية المخصّصة لـ Google Studio **المقصوصة عند 300 فاتورة**.

**التعديل:** عزل تحميل كل shard في try/catch مستقل مع إعادة قراءة نظيفة (manifest+parts) عند الفشل — في `initBootCache` (~سطر 1918) وفي المستمع الحيّ per-shard (~سطر 1969). shard واحد فاشل لم يعد يسمّم الكاش، فيرجع الأرشيف كامل.
