# تقرير التحقق — 2026-08-29 (تصحيح لتقريري السابق)

راجعت الإنتاج وقاعدة البيانات مباشرة. **تشخيصي السابق كان خاطئاً في الجزئيتين**، وهذا التصحيح المبني على أدلة فعلية.

---

## ١) سجل الفواتير — «وين الباقي؟»

### الواقع المُقاس (لا تخمين)
| المصدر | عدد الفواتير |
|---|---|
| `/api/appdata/full` (السيرفر المباشر) | **9** |
| shard الفواتير | **9** |
| مجموعة `invoices` في Firestore | **9** |
| الوثيقة الجذرية | **9** |

الفواتير التسع كلها **INV-5089 → INV-5097** (٢٧ يوليو ← ٢٩ أغسطس). كل ما قبل INV-5089 **غير موجود في قاعدة البيانات إطلاقاً**.

### السبب الجذري
ليس سقف الـ 300 كما قلت سابقاً — **البيانات نفسها فُقدت من قاعدة البيانات يوم 2026-07-27.**

وجدت في `appData/shared_company_data/shards` أربعة **أجزاء يتيمة** (`invoices__v2__*`, ~600KB لكل جزء) بتواريخ 27 يوليو (12:44، 20:26، 20:27، 20:28). كل جيل يحتاج جزأين (`partCount: 2`) ولا يوجد جيل واحد مكتمل — أي أن عملية حفظ الـ shard المجزّأ انكسرت في المنتصف، فضاع الأرشيف واستُبدل بـ shard صغير يحمل ما كان في ذاكرة المتصفح فقط.

### الاسترجاع — نجح جزئياً ✅
فككت ضغط الأجزاء اليتيمة واستخرجت السجلات السليمة:
- **71 فاتورة مسترجعة**: INV-5001 → INV-5089
- المدى الزمني: 2026-03-31 → 2026-07-27
- المجموع: **4124.250 د.ك**
- كل السجلات كاملة (بنود، عميل، عنوان، مبالغ)
- **18 رقماً مفقوداً** داخل المدى (تلف الذيل): 5009، 5010، 5013، 5016، 5020، 5021، 5042، 5068، 5070، 5075، 5076، 5078، 5080، 5081، 5082، 5085، 5086، 5087

الملف: **`recovered_invoices.json`** (مرفق).
يوجد أيضاً أرشيف أقدم في مجموعة `appData_invoices` فيه 41 فاتورة بتاريخ 18 يونيو.

### لا يوجد طريق استرجاع آخر
- نسخ احتياطية Firestore: **لا توجد**
- PITR: **معطّل** (أقدم نسخة متاحة عمرها ساعة فقط)

⚠️ **لم أستورد أي شيء** — الاسترجاع يكتب على بيانات حيّة، وينتظر موافقتك.

---

## ٢) الإشعارات — «تم دفع فاتورة» مرتين

### الواقع المُقاس
سجل `pushEvents` لـ INV-5097 فيه **حدث إرسال واحد فقط**:
```
safe-worker-invoice-paid-INV-5097@202608291503
source=payment-confirm-instant  status=sent
successCount: 6   failureCount: 0   tokensCount: 6
```

**فنظريتي السابقة (مسارَي إرسال) خاطئة** — لا يوجد حدث ثانٍ. المسار الـ inline لم يعمل أصلاً لأن `paymentStatus` كان قد صار `paid` قبله.

### السبب الحقيقي
الإشعار أُرسل مرة واحدة إلى **6 توكنات = 6 تسجيلات أجهزة مختلفة**، وكلها `deviceId` متمايز:

| # | الحساب | الجهاز | الشاشة |
|---|---|---|---|
| 1 | mfq241188@gmail.com | iPhone (PWA) | 420×912 |
| 2 | volcanokw@gmail.com | Mac Safari | 1440×900 |
| 3 | volcanokw@gmail.com | Mac Safari | 1440×900 |
| 4 | volcanokw@gmail.com | Mac Chrome | 1440×900 |
| 5 | omaralawadhi67@gmail.com | iPhone (PWA) | 428×926 |
| 6 | volcanokw@gmail.com | iPhone (PWA) | 420×912 |

**التسجيلان 1 و 6 متطابقان تماماً** في الشاشة (420×912 — وهو مقاس «تكبير العرض» غير الشائع) ونظام iOS 18.7 ونمط PWA، لكن بحسابين مختلفين ومعرّفَي جهاز مختلفين.

الاستنتاج الأرجح: **التطبيق مثبّت مرتين على نفس الآيفون** (أو تثبيت واحد سُجّل بحسابين)، فصار لهاتفك تسجيلان حيّان. الوسم `payment-final-state-invoice-INV-5097` يدمج الإشعارات المتطابقة **داخل التثبيت الواحد فقط**، ولا يدمج عبر تثبيتين — فظهر الإشعار مرتين.

### لماذا لم أصلحه بالكود
جرّبت إصلاحاً يدمج الأجهزة ببصمة (نظام/شاشة/متصفح) وقِسته على البيانات الحقيقية: **لم يُلغِ ولا تسجيلاً واحداً**، ولو ضبطته ليدمج عبر الحسابات لكان سيوقف إشعارات جهاز شرعي (مثلاً Mac Safari رقم 2 و3 يختلفان بـ 6 بكسل فقط). **فتراجعت عنه بالكامل** — إصلاح يُسكت إشعارات حقيقية أسوأ من الازدواج.

### الحل الصحيح (تشغيلي، بيدك)
احذف التسجيل الزائد لهاتفك — إمّا بحذف نسخة PWA المكرّرة من الشاشة الرئيسية، أو بتعطيل التوكن الزائد:
- الزائد المرشّح: `fBHAqtnZnVgmx90ys5VggS…` (mfq241188) أو `c82B_ze_RTWR56ZoxfVVIw…` (volcanokw) — أبقِ الحساب الذي تستخدمه فعلياً.
- بعدها يصبح الإشعار مرة واحدة دون أي تغيير بالكود.

---

## التعديلات في server.ts (تحصين فقط — ليست علاج المشكلتين)

الملف المعدّل الوحيد: **server.ts**. لم أرفع أي شيء ولم أنشر.

1. **`initBootCache` + مستمع الـ shard الحيّ**: عزل تحميل كل shard في try/catch مستقل مع إعادة قراءة نظيفة عند الفشل. سابقاً كان فشل shard واحد يُفشل الكاش كله. هذا **يمنع تكرار كارثة 27 يوليو**، لكنه لا يُرجع البيانات المفقودة.

2. **حذف الإرسال الـ inline المكرر في `handlePaymentUpdate`** (فاتورة وطلب): كان يستخدم معرّف حدث بلا لاحقة زمنية، فلو عمل يوماً لأنتج إشعاراً مكرراً حقيقياً. **لم يكن سبب مشكلتك الحالية** — حذفه تنظيف وقائي فقط.

⚠️ التعديلان في السيرفر ويحتاجان إعادة نشر. لم أتمكن من فحص build (TypeScript غير مثبّت محلياً).

---

## GitHub
- مجلد العمل `Downloads/Websites/Admin` **ليس مستودع git** (لا `.git` ولا remote) — لا يمكن عمل diff أو push منه.
- الحساب `ProfAlfailakawi` فيه `Admin-New-2026` (آخر دفعة 2026-08-29 18:10) و`Order-New-2026`. لم أرفع ولم أعدّل أي مستودع.
