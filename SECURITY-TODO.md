# SECURITY-TODO — إصلاحات مؤجّلة تحتاج موافقة المالك

هذه القائمة تسجيلية فقط. **لم يُطبَّق أي إصلاح** على منطق الدفع أو القواعد
أو نقاط الـAPI أو المصادقة في هذه الجولة. جولة التنظيف اقتصرت على حذف الملفات
الميتة والمكرّرة والبيانات الشخصية من شجرة العمل. البنود أدناه تحتاج قراراً
ومراجعة منفصلة من المالك.

## 1) بيانات العملاء المحذوفة تحتاج تنظيف تاريخ git

حُذفت من شجرة العمل ملفات فيها بيانات شخصية حقيقية (أسماء/هواتف/عناوين/فواتير):

- `MERGED_complete_88.json` (‏~1.29MB، ‏88 فاتورة حقيقية)
- `recovered_invoices.json` (‏~660KB، ‏71 فاتورة بعناوين)

الحذف من الشجرة **لا يزيلها من تاريخ git** — النسخ القديمة ما زالت قابلة للاسترجاع
من الـcommits السابقة. التنظيف الكامل يتطلب إعادة كتابة التاريخ
(‏`git filter-repo` أو مكافئه) ثم `force-push`، وهو إجراء مدمّر يحتاج **موافقة
المالك الصريحة** وتنسيقاً مع كل من يملك نسخة من المستودع. لم يُنفَّذ هنا التزاماً
بقاعدة «عدم إعادة كتابة التاريخ».

كذلك يُنصح، بعد الموافقة، بمراجعة أي أسرار قد تكون ظهرت في التاريخ (مفاتيح، VAPID،
توكنات) وتدويرها إن لزم.

## 2) دفع حيّ وقواعد مفتوحة — تُركت عمداً بطلب المالك

بطلب صريح من المالك (المطبخ فيه **دفع حيّ** ولا يجوز المخاطرة بكسر التشغيل)، تُركت
العناصر التالية **كما هي دون أي تعديل**، وتحتاج مراجعة أمنية منفصلة لاحقاً بموافقته:

- **ويبهوك الدفع ومنطق الطلبات** داخل `server.ts` (السيرفر الحيّ) — لم يُلمس.
- **قواعد Firestore** في `firestore.rules` — متروكة مفتوحة عمداً كما هي.
- **قواعد التخزين** في `storage.rules` — لم تُلمس.
- **نقاط الـAPI المفتوحة** والمصادقة — لم تُلمس.

مراجعة لاحقة مقترحة (بعد موافقة المالك وبيئة اختبار لا تمسّ الدفع الحيّ):
تضييق قواعد Firestore/Storage، وإضافة تحقّق/توثيق على نقاط الـAPI الحساسة،
والتأكد من التحقق من توقيع ويبهوك الدفع.
