*** Begin Patch
*** Update File: server.ts
@@
-  app.post("/api/smart-studio/generate", express.json({ limit: '50mb' }), async (req, res) => {
+  const SMART_STUDIO_SCENE_LIBRARY: Record<string, string> = {
+    heritage_table: "طاولة تقديم كويتية راقية: سدو ناعم، دلة نحاس بعيدة وغير مسيطرة، بخور خفيف جداً، خامات خشب وحجر دافئة.",
+    modern_kuwait_cafe: "كافيه كويتي حديث: رخام أو خشب نظيف، ضوء نهاري طبيعي، إحساس مدينة الكويت المعاصر من غير شعارات أو نصوص.",
+    seaside_sunset: "أجواء بحرية كويتية هادئة: ضوء غروب ناعم، انعكاس بسيط، خامات رملية/حجرية، بدون مبالغة سياحية.",
+    luxury_dark: "لقطة فاخرة داكنة: أسطح فخمة، إضاءة جانبية سينمائية، انعكاسات خفيفة، تركيز كامل على الطبق.",
+    clean_catalog: "تصوير قائمة طعام نظيف: خلفية محايدة، إضاءة ناعمة، تركيز تجاري واضح، بدون عناصر تراثية أو مشهدية كثيرة."
+  };
+
+  const SMART_STUDIO_STYLE_LIBRARY: Record<string, string> = {
+    professional_food_photo: "تصوير فوتوغرافي احترافي لقائمة طعام: واقعية عالية، عدسة طعام، عمق ميدان طبيعي، ألوان شهية، بدون تغيير الطبق.",
+    kuwaiti_inspired_scene: "بيئة واقعية مستوحاة من الكويت: تضيف سياقاً مكانياً وخامات محلية راقية حول الطبق، وليست مجرد فلتر تصوير.",
+    social_media_hero: "لقطة بطل للسوشيال ميديا: تكوين جذاب، مساحة نظيفة حول الطبق، قابل للاستخدام كإعلان أو منشور.",
+    premium_menu: "لقطة منيو فاخرة: هادئة، نظيفة، تجارية، تجعل الطبق مناسباً لقائمة مطعم راقٍ."
+  };
+
+  const normalizeSmartStudioChoice = (value: any) => String(value || "").trim().toLowerCase();
+
+  const resolveSmartStudioScene = (theme: any, sceneKey: any) => {
+    const key = normalizeSmartStudioChoice(sceneKey || theme);
+    if (SMART_STUDIO_SCENE_LIBRARY[key]) return SMART_STUDIO_SCENE_LIBRARY[key];
+    if (["تراثي", "heritage", "sadu"].includes(key)) return SMART_STUDIO_SCENE_LIBRARY.heritage_table;
+    if (["مودرن كافيه", "modern", "cafe", "kuwait cafe"].includes(key)) return SMART_STUDIO_SCENE_LIBRARY.modern_kuwait_cafe;
+    if (["بحر", "sea", "seaside", "beach"].includes(key)) return SMART_STUDIO_SCENE_LIBRARY.seaside_sunset;
+    if (["فاخر", "luxury", "dark"].includes(key)) return SMART_STUDIO_SCENE_LIBRARY.luxury_dark;
+    return SMART_STUDIO_SCENE_LIBRARY.clean_catalog;
+  };
+
+  const resolveSmartStudioStyle = (mode: any, mood: any) => {
+    const key = normalizeSmartStudioChoice(mode || mood);
+    if (SMART_STUDIO_STYLE_LIBRARY[key]) return SMART_STUDIO_STYLE_LIBRARY[key];
+    if (["مشهد كويتي", "kuwaiti", "kuwait", "local"].includes(key)) return SMART_STUDIO_STYLE_LIBRARY.kuwaiti_inspired_scene;
+    if (["تصوير احترافي", "professional", "photo", "photography"].includes(key)) return SMART_STUDIO_STYLE_LIBRARY.professional_food_photo;
+    if (["social", "hero", "ad"].includes(key)) return SMART_STUDIO_STYLE_LIBRARY.social_media_hero;
+    return SMART_STUDIO_STYLE_LIBRARY.premium_menu;
+  };
+
+  const buildSmartStudioPrompt = ({ idea, theme, mood, sceneKey, mode }: any) => {
+    const userIdea = String(idea || "").trim();
+    const sceneInstruction = resolveSmartStudioScene(theme, sceneKey);
+    const styleInstruction = resolveSmartStudioStyle(mode, mood);
+
+    return `بناءً على الصورة المرفقة للطبق، قم بتوليد صورة طعام فوتوغرافية واقعية جداً وجاهزة للاستخدام التجاري.
+
+فكرة المستخدم الاختيارية:
+${userIdea ? `- نفّذ هذه الفكرة أولاً ما دامت لا تغيّر الطبق: ${userIdea}` : "- لا توجد فكرة مكتوبة من المستخدم؛ ولّد مباشرة بأفضل تكوين مناسب اعتماداً على الاختيارات."}
+
+تعريف المسارات حتى لا تتكرر الأسماء:
+- البيئة المستوحاة من الكويت = سياق مكاني وخامات محلية راقية حول الطبق.
+- التصوير الاحترافي = جودة الصورة والعدسة والإضاءة والتكوين التجاري، وقد يعمل مع أي بيئة.
+
+القواعد الصارمة:
+- الطبق هو البطل: حافظ تماماً على شكله، مكوناته، حجمه النسبي، وطريقة تقديمه دون تغيير أو إضافة مكونات غير موجودة.
+- لا تغيّر نوع الطعام ولا تخلط أطباقاً جديدة.
+- لا تضف نصوصاً، حروفاً، شعارات، علامات مائية، توقيعات، ملصقات، أو كتابات داخل الصورة نهائياً.
+- لا تجعل العناصر الكويتية تسرق الانتباه من الطبق؛ تكون داعمة وخلفية فقط.
+- صورة فوتوغرافية حقيقية وليست رسماً، وليست 3D مبالغاً فيه.
+
+المكتبة المدمجة المختارة:
+- المشهد: ${sceneInstruction}
+- الأسلوب: ${styleInstruction}
+- المزاج/الإضاءة: ${mood || "دافئ واحترافي"}
+
+الهدف النهائي:
+صورة طعام مذهلة وواقعية، تصلح للمنيو والسوشيال ميديا، مع احترام فكرة المستخدم إذا كتبها، والتوليد المباشر إذا تركها فارغة.`;
+  };
+
+  app.post("/api/smart-studio/generate", express.json({ limit: '50mb' }), async (req, res) => {
     try {
-      const { imageContent, mimeType, format, theme, mood } = req.body;
+      const { imageContent, mimeType, format, theme, mood, idea, userIdea, prompt, sceneKey, mode } = req.body;
       if (!imageContent) return res.status(400).json({ error: "Missing image" });
       
-      const systemInstruction = "أنت مدير فني عالمي متخصص في تصوير الأطعمة للمجلات الراقية والسوشيال ميديا، خبير في البيئة والجماليات الكويتية.";
-      let autoPrompt = `بناءً على الصورة المرفقة للطبق، قم بتوليد عمل فني إبداعي فائق الواقعية (Ultra-Realistic 8K Professional Photography).
- القواعد الصارمة (STRICT RULES):
-- الطبق (Star of the show): حافظ تماماً على شكله، مكوناته، وطريقة تقديمه دون أي تغيير (Zero Hallucinations).
-- البيئة والمحيط: صمم خلفية تدعم "واقعية 10000%" للمشهد في بيئة كويتية راقية حقيقية. يجب أن تبدو صورة فوتوغرافية حقيقية تماماً وليست رسمة.
+      const systemInstruction = "أنت مدير تصوير طعام ومدير فني تجاري. نفّذ فكرة المستخدم إذا وجدت، وإذا لم توجد ولّد مباشرة. فرّق دائماً بين جودة التصوير وبين البيئة المستوحاة من الكويت.";
+      let autoPrompt = buildSmartStudioPrompt({
+        idea: idea || userIdea || prompt,
+        theme,
+        mood,
+        sceneKey,
+        mode
+      });
*** End Patch
