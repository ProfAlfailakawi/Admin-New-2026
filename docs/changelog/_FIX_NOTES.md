# تقرير 2026-08-29 — استعادة الفواتير + تشخيص الإشعارات

## ✅ النتيجة: الأرشيف رجع كامل

| | قبل | بعد |
|---|---|---|
| سجل الفواتير | 9 | **88** |
| المدى | INV-5089→5097 | **INV-5001→5097** |
| القيمة | ~626 د.ك | **4877.150 د.ك** |

تم التحقق من ٣ مصادر مستقلة: مجموعة `invoices` = 88، الـshard عبر السيرفر = 88، ودمج ReportsPage = 88.

---

## ١) «١٨ فاتورة ضاعت وين راحت؟» — الجواب

بعد مطابقة الباك اب `KTK_Full_Backup_2026-07-27.xlsx` مع ما انتشلته من الـshards:

**٩ منها كانت موجودة في الباك اب واستُعيدت** ✅
5075، 5076، 5078، 5080، 5081، 5082، 5085، 5086، 5087

**٩ منها لم تُفقد أصلاً — لم تُنشأ قط** ✅
5009، 5010، 5013، 5016، 5020، 5021، 5042، 5068، 5070
هذي الأرقام **غير موجودة في أي مصدر إطلاقاً**، بما فيها الباك اب المأخوذ قبل الكارثة بساعات. أي أنها فجوات ترقيم طبيعية (فواتير أُلغيت أو أرقام قُفزت)، وليست بيانات ضائعة.

**الخلاصة: لم يضع ولا سجل واحد.** الأرشيف كامل ١٠٠٪.

### تحقق مزدوج للسلامة
قارنت الباك اب (13:58) مع المنتشَل من الـshards (20:28) على ٧٠ فاتورة مشتركة:
- اختلافات في المبالغ: **صفر**
- اختلافات في التواريخ: **صفر**
- اختلافات في حالة الدفع/الحالة: **صفر**
- البنود (items): متطابقة ٧٠/٧٠

مصدران مستقلان تماماً تطابقا حرفياً — أقوى تحقق ممكن.

---

## ٢) سبب الكارثة الأصلية (٢٧ يوليو)

عملية حفظ الـshard المجزّأ انكسرت في منتصفها؛ خلّفت ٤ أجزاء يتيمة (~600KB لكل جزء) بلا جيل مكتمل، فضاع الأرشيف واستُبدل بنسخة صغيرة.

آلية القراءة `loadFullAppDataShard` **ترمي استثناء** عند أي عدم تطابق لحظي بين الـmanifest والأجزاء، وكل الـshards كانت تُحمَّل في `Promise.all` واحد — ففشل shard واحد كان يُسقط الكاش كله.

**التعديل في server.ts** (تحصين، غير منشور بعد):
- `initBootCache`: عزل تحميل كل shard في try/catch مستقل + إعادة قراءة نظيفة عند الفشل.
- المستمع الحيّ per-shard: نفس منطق إعادة المحاولة.
- حذف إرسال إشعار inline مكرر خامل في `handlePaymentUpdate`.

⚠️ يحتاج إعادة نشر السيرفر. (لم أتمكن من فحص build — TypeScript غير مثبّت محلياً.)

---

## ٣) الإشعارات المكررة — التشخيص النهائي

سجل `pushEvents` لـ INV-5097 فيه **حدث إرسال واحد فقط**:
```
successCount: 6   failureCount: 0   tokensCount: 6
```

فليست مشكلة «مسارَي إرسال». الإشعار أُرسل مرة واحدة إلى **٦ تسجيلات أجهزة مختلفة**، منها **تسجيلان لآيفون متطابقين تماماً** (شاشة 420×912، iOS 18.7، نمط PWA) بحسابين مختلفين — أي **التطبيق مسجّل مرتين على نفس هاتفك**.

الوسم `payment-final-state-invoice-INV-5097` يدمج الإشعارات داخل التثبيت الواحد فقط، لا عبر تثبيتين → ظهر مرتين.

**الحل (تشغيلي، بيدك):** احذف نسخة PWA المكررة، أو عطّل التوكن الزائد:
`fBHAqtnZnVgmx90ys5VggS…` (mfq241188) أو `c82B_ze_RTWR56ZoxfVVIw…` (volcanokw) — أبقِ الحساب المستخدم فعلاً.

**لماذا لم أصلحه بالكود:** بنيت دمجاً ببصمة الجهاز وقِسته على البيانات الحقيقية — لم يُلغِ ولا تسجيلاً، ولو وسّعته ليدمج عبر الحسابات لأوقف إشعارات جهاز شرعي (جهازا Mac يفرقان ٦ بكسل). فتراجعت عنه بالكامل.

---

## ⚠️ مخاطر قائمة — يُنصح بمعالجتها

1. **لا توجد نسخ احتياطية و PITR معطّل** — أقدم نسخة متاحة عمرها ساعة. لو تكررت الكارثة اليوم، لا يوجد ما يُنقذ. يُنصح بتفعيل PITR وجدولة نسخ تلقائية.
2. **الـshard عاد مجزّأً** (2 أجزاء، 1,030,676 بايت) — نفس الآلية التي انكسرت في يوليو. نشر تعديل server.ts يحصّن جانب القراءة.
3. **الوثيقة الجذرية تحمل 58 فاتورة فقط** (نسخة Google Studio تتقلص تحت 1MiB) — طبيعي، لأن المصدر الرسمي هو الـshard.

---

## الملفات
- `server.ts` — التحصين (غير منشور)
- `MERGED_complete_88.json` — الأرشيف الكامل ٨٨ فاتورة
- مجلد `alturath-SAFETY-BACKUP-before-restore/` على الـDesktop — حالة ما قبل الاستعادة + كل المصادر

## GitHub
مجلد العمل ليس git repo (لا `.git`). الحساب فيه `Admin-New-2026` و`Order-New-2026`. **لم أرفع ولم أعدّل أي مستودع.**
