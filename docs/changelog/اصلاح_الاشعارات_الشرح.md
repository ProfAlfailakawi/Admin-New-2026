# إصلاح إشعارات الدفع — السبب الجذري والعلاج (مُتحقق منه حياً)

## القنبلتان اللتان أسكتتا الإشعارات
1. **أرقام الفواتير معاد تدويرها**: النظام مرّ بحقبة قديمة وصلت `INV-5286` ثم انعاد العدّاد لمنطقة الـ5000. سجل منع التكرار يحفظ «أرسلتُ إشعار INV-XXXX» للأبد — فالفاتورة الجديدة برقم مستعمل تُحسب «مرسلة سابقاً» ويسكت عنها النظام بصمت، في **المسارات الثلاثة كلها** (المذيع الفوري لحظة الدفع + المحرك الدوري كل 5 ثوانٍ + المسار القديم).
   - الدليل: بصمة `INV-5090` القديمة (26 يوليو — 0.500 د.ك) خنقت إشعار «تم الدفع» لفاتورتكم الحقيقية (29 يوليو — 113.350 د.ك) → اكتشفتم الدفع يدوياً.
2. **فواتير توصيل الغد تُختم بتاريخ الغد**: `createdAt` يأخذ يوم التوصيل بدل لحظة الإنشاء، وشرط «لم يدفع» هو «مر 10/30 دقيقة على الإنشاء» — وفاتورة مؤرخة بالمستقبل لا يمر عليها وقت أبداً → صمت `checked: 0`.
   - الدليل: `INV-5091` أُنشئت 30/07 الساعة 14:26 وتوصيلها 31/07 → ختمها 31/07!

وقنبلة ثالثة صغيرة: حجوزات عالقة بحالة "claimed" (محجوزة ولم تُرسل قط) كانت تسد أرقاماً قبل حتى أول استخدام حقيقي.

## العلاج الجراحي (ملف واحد: server.ts)
1. **بصمة ميلاد لكل فاتورة** — دالة `paymentAlertEraSuffix`: معرف كل إشعار صار `INV-5091@202607301126` (رقم الفاتورة + لحظة إصدارها الحقيقية). الرقم المدوَّر ياخذ بصمة نظيفة خاصة به، وكل الأشباح والحجوزات العالقة القديمة فقدت مفعولها للأبد. طُبّقت في المذيع الفوري وفي كل مواضع المحرك الدوري (9 مواضع) — والمساران يشتقان نفس المعرف حرفياً فيبقى منع التكرار بينهما محكماً.
2. **ترياق تاريخ الغد** — في دالة قراءة التاريخ: إذا كان التاريخ مستقبلياً → استخدم لحظة الإصدار الحقيقية `issuedAt`.

**ما لم يُلمس**: الدفع نفسه، منطق الإرسال، منع التكرار بالأجهزة (SW)، التوكنات، الجداول، الواتساب.

## التحقق الحي بعد النشر (مقاس، مو كلام)
- أول دورة للمحرك بعد النشر: `Auto runner sent: 2` — **تنبيها INV-5091 المحبوسان («لم تُدفع» + «لم تُدفع بعد 30 دقيقة») انطلقا فوراً** بمعرفيهما الجديدين `@202607301126`، ووصلا لثلاثة أجهزة (successCount: 3)
- الدورات التالية: عادت `checked: 0` — **صفر تكرار، صفر فيضان**
- جسر الواتساب: سليم لم يتأثر

## ضمانة المستقبل (سؤالك: «لا يتعلق شي»)
| خطر مستقبلي | الحماية الآن |
|---|---|
| رقم فاتورة معاد استخدامه | بصمة الميلاد تميّزه تلقائياً — مستحيل يرث سجل رقم قديم |
| فاتورة بتوصيل مؤجل (بكرة أو بعده) | تُقاس من لحظة إصدارها الحقيقية |
| حجز علق بسبب انقطاع لحظة الإرسال | يُحرَّر تلقائياً ليُعاد إرساله (موجود أصلاً + البصمة تحمي من العالق القديم) |
| إعادة تشغيل/نشر السيرفر | المحرك يقلع تلقائياً كل مرة (يعمل كل 5 ثوانٍ داخلياً) والمؤقت السحابي احتياط خارجي |

## ملاحظتان مستقبليتان (خارج نطاق هذا الإصلاح — قراركم لاحقاً)
1. **عدّاد الفواتير نفسه يعيد الأرقام** — الإشعارات صارت محصنة ضده، لكن الأفضل مستقبلاً إصلاح العدّاد نفسه (تفادياً لأي لبس محاسبي بين فاتورتين بنفس الرقم).
2. **تنبيه «لم يدفع» قد يتأخر عن الـ10 دقايق** إذا ما كان أحد فاتحاً لوحة التحكم وقتها (مرآة البيانات تتحدث عند فتح اللوحة) — هذا سلوك قديم موجود من قبل، والمذيع الفوري يغطي «تم الدفع» دائماً.

*الملف المعدل: server.ts فقط — منشور على Cloud Run (مراجعة service-00772) — ولا شيء رُفع على GitHub.*
