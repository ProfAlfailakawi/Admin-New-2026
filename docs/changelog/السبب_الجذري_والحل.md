# ليش الواتساب كان يحتاج الكمبيوتر شغال — والحل

## السبب الجذري
البوت له «جسر» يربط واتساب بالسيرفر. الجسر كان يشتغل على سيرفر سحابي (VM باسم `alturath-bridge` على Google Cloud) — لهذا كان يشتغل 24 ساعة حتى والكمبيوتر مسكّر.

يوم **26 يوليو** تم إيقاف وتعطيل الجسر السحابي، لأن نسخته القديمة كانت تعاني من مشكلة «البوت يوقّف نفسه» (يحسب ردوده الآلية ردوداً يدوية فيسكت). وانتقل التشغيل لجهاز الماك بالكود الجديد المُصلَّح — فصار الواتساب يعتمد على بقاء الكمبيوتر شغال.

## الحل (بدون أي تعديل على الإشعارات أو الدفع أو Cloud Run)
1. **تحديث كود الجسر على السيرفر السحابي** بنفس النسخة الجديدة الشغالة (المطابقة تماماً لـ GitHub) — ملفّا `index.mjs` و `service-runner.mjs` المرفقان.
2. **تصحيح كلمة سر الجسر** على السيرفر: كانت قديمة (تغيّرت بعد الإصلاح الأمني)، وسُحبت القيمة الحقيقية من إعدادات Cloud Run ووُضعت في `.env` على السيرفر (غير مرفقة هنا لأنها سرّية).
3. **نقل جلسة واتساب الحالية** (اللي مُسح رمزها حديثاً) من الماك إلى السيرفر — نفس الربط، بدون الحاجة لمسح QR جديد.
4. **تفعيل الإقلاع التلقائي** على السيرفر (`systemctl enabled`) — يرجع يشتغل بروحه حتى لو أعيد تشغيل السيرفر.
5. **تعطيل التشغيل التلقائي على الماك** حتى ما يصير جسران شغالين بنفس الوقت (يسبب ردود مزدوجة وتوقف البوت). ملف التشغيل محفوظ باسم:
   `~/Library/LaunchAgents/com.alturathkw.whatsapp-bridge.plist.disabled-moved-to-cloud-20260730`

## النتيجة (تم التحقق)
- الجسر السحابي: **ready + online، صفر أخطاء قراءة طابور، النبضة تصل كل ثوانٍ**، الرقم مرتبط.
- الواتساب الآن يشتغل **والكمبيوتر مسكّر** — مثل قبل، لكن بالكود الجديد المحترف.
- جسر عيادة د. أحمد (نظام منفصل) لم يُلمس وما زال شغالاً.

## الملفات المعدلة (على السيرفر السحابي فقط — لا تغيير بملفات المشروع محلياً ولا على GitHub)
| الملف | التغيير |
|---|---|
| `index.mjs` | استُبدل بالنسخة الجديدة (مطابقة لـ GitHub) |
| `service-runner.mjs` | استُبدل بالنسخة الجديدة (مطابقة لـ GitHub) |
| `.env` على السيرفر | تحديث كلمة السر فقط (سرّية — غير مرفقة) |

## تنبيه مهم للمستقبل
لا تشغّل جسر الماك والجسر السحابي بنفس الوقت — الجلسة صارت وحدة مشتركة. لو احتجت ترجع للماك يوماً: أوقف السحابي أولاً (`sudo systemctl stop alturath-bridge`) ثم أعد تسمية ملف الـ plist وشغّله.
