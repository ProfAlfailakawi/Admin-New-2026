# ملخّص التعديلات — مشروع Admin (Admin-New-2026)
تعديل محلي فقط (لم يُنشر). لم تُلمس FCM ولا الدفع. متحقّق: `tsc` + `vite build` أخضر.
اسم الملف مُرمّز بمساره داخل المشروع (استبدل `__` بـ`/`).

## الملف المعدّل (1)

### src__components__OrderPage.tsx  →  المكان: `src/components/OrderPage.tsx`
- **السبب الجذري:** سجلّات `console.log("DEBUG...")` متروكة في الإنتاج.
- **التغيير:** حذف سجلّين خاصّين بفحص الموردين فقط — بدون أي تغيير منطق.
- **ملاحظة:** تُركت 3 سجلّات ملاصقة للدفع/الفواتير في نفس الملف عمداً (بانتظار الإذن).

## للتطبيق
انسخ الملف إلى `src/components/OrderPage.tsx` (استبدال)، ثم `npm run build`.
